extends layout.jade

block content
	h4 Profile
	h5 You are logged in as #{user.username}

	if(votes.length>0)
		h5 You've RSVPd
		ul
			each vote in votes
				li #{vote.barname}
					a(href='/api/notgoing/place/' + vote.bar + '/' +vote.barname) not going!
	else
		h5 You haven't RSVPd!