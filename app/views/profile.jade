extends layout.jade

block content
	h1 Profile
	h2 Welcome #{user.username}
	
	if(votes.length>0)
		p You've RSVPd
		ul
			each vote in votes
				li
					p #{vote.barname}
					a(href='/api/notgoing/place/' + vote.bar + '/' +vote.barname) not going!